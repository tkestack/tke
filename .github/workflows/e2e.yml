name: e2e

on:
  push:
    branches:
      - master

  pull_request_target:
    branches:
      - master
env:
  VERSION: ${{ github.sha }}
  BUILDER: ${{ secrets.BUILDER }}
  KUBECONFIG: ${{ secrets.KUBECONFIG }}
  REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
  REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
  SECRET_ID: ${{ secrets.SECRET_ID }}
  SECRET_KEY: ${{ secrets.SECRET_KEY }}
  REGION: ${{ secrets.REGION }}
  CREATE_INSTANCES_PARAM: ${{ secrets.CREATE_INSTANCES_PARAM }}
  PASSWORD: ${{ secrets.PASSWORD }}
  NEED_DELETE: ${{ secrets.NEED_DELETE }}
  DOCKER_HUB_ACTION_AUTH: ${{ secrets.DOCKER_HUB_ACTION_AUTH }}
  INSTANCE_TYPES: ${{ secrets.INSTANCE_TYPES }}
  TEST1: ${{ secrets.TEST.TEST1 }}
jobs:
  image:
    name: build tke image
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: build and push platform image
        run: |
          make push IMAGES="tke-platform-api tke-platform-controller" VERSION=${{ github.sha }}
  platform1:
    name: platform test - 1.19.7
    needs: image
    runs-on: self-hosted
    env:
      K8SVERSION: 1.19.7
    steps:
      - name: printversion
        env:
          LABELNAME_CONTEXT: ${{ toJson(github.event.pull_request_target.labels.*.name) }}
        run: |
          echo "$LABELNAME_CONTEXT"
      - name: e2e test
        run: |
          ginkgo -nodes=3 -v test/e2e/platform
