apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-auth-api.fullname" . }}
  labels:
    {{- include "tke-auth-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-auth-api.selectorLabels" . | nindent 4 }}
