apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-notify-controller.fullname" . }}
  labels:
    {{- include "tke-notify-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-notify-controller.selectorLabels" . | nindent 4 }}
