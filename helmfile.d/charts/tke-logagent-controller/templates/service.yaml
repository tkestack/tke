apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-logagent-controller.fullname" . }}
  labels:
    {{- include "tke-logagent-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-logagent-controller.selectorLabels" . | nindent 4 }}
