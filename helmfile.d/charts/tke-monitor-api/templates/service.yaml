apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-monitor-api.fullname" . }}
  labels:
    {{- include "tke-monitor-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-monitor-api.selectorLabels" . | nindent 4 }}
