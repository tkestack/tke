apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-business-controller.fullname" . }}
  labels:
    {{- include "tke-business-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-business-controller.selectorLabels" . | nindent 4 }}
