apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tke-business-controller.fullname" . }}-test-connection"
  labels:
    {{- include "tke-business-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: curl
      image: {{ .Values.registryPrefix }}/toolbox:v0.1.0
      command:
        - sh
        - -c
        - "curl -k https://{{ include "tke-business-controller.fullname" . }}:{{ .Values.service.port }}/healthz"
  restartPolicy: Never
