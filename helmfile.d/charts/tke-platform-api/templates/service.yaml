apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-platform-api.fullname" . }}
  labels:
    {{- include "tke-platform-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-platform-api.selectorLabels" . | nindent 4 }}
