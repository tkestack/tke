apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-business-api.fullname" . }}
  labels:
    {{- include "tke-business-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-business-api.selectorLabels" . | nindent 4 }}
