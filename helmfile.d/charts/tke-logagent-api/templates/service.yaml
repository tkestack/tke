apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-logagent-api.fullname" . }}
  labels:
    {{- include "tke-logagent-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-logagent-api.selectorLabels" . | nindent 4 }}
