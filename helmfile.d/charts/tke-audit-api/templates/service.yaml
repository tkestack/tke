apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-audit-api.fullname" . }}
  labels:
    {{- include "tke-audit-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-audit-api.selectorLabels" . | nindent 4 }}
