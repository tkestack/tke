apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-registry-api.fullname" . }}
  labels:
    {{- include "tke-registry-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-registry-api.selectorLabels" . | nindent 4 }}
