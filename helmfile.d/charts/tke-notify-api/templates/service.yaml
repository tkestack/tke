apiVersion: v1
kind: Service
metadata:
  name: {{ include "tke-notify-api.fullname" . }}
  labels:
    {{- include "tke-notify-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tke-notify-api.selectorLabels" . | nindent 4 }}
